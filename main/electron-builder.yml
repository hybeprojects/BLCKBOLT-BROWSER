publish:
  provider: github
  owner: BLCKBOLT-ORG
  repo: blckbolt-browser

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  artifactName: "BLCKBOLT-Setup-${version}.exe"
  installerSidebar: "build/installerSidebar.bmp"
  uninstallDisplayName: "BLCKBOLT Browser"
  perMachine: true
